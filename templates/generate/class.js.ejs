import {EdmMapping,EdmType} from '@themost/data';
import {DataObject} from '@themost/data';
<% if (inherits) { %>import {<%-inherits%>} from '<%-inheritsClassPath%>';<% } %>

@EdmMapping.entityType('<%-name%>')
class <%-name%> extends <%-inherits ? inherits : "DataObject"%> {
<% attributes.filter(function(attribute) { return attribute.model === name; }).forEach(function(attribute) { %>
    /** @type {<%=attribute.typeName%>} */
    <%-attribute.property || attribute.name%>;<% }); %>
    /**
     * @constructor
     */
    constructor() {
        super();
    }
}
export {
    <%-name%>
}