import {EdmMapping,EdmType} from '@themost/data/odata';
import {DataObject} from '@themost/data/data-object';
<% if (inherits) { %>let <%-inherits%> = require('<%-inheritsClassPath%>');<% }%>
/**
 * @class
 <% attributes.filter(function(x) { return x.model === name; }).forEach(function(x) { %>
 * @property {<%-x.typeName%>} <%-x.property || x.name%><% }) %>
 * @augments {DataObject}
 */
@EdmMapping.entityType('<%-name%>')
class <%-name%> extends <%-inherits ? inherits : "DataObject"%> {
    /**
     * @constructor
     */
    constructor() {
        super();
    }
}
module.exports = <%-name%>;