import {EdmMapping,EdmType} from '@themost/data/odata';
<% locals.imports.forEach(function(x) { if (x.name==='{DataObject}') { %>
import <%=x.name%> from '<%=x.from%>';<% } else { %>import <%=x.name%> = require('<%=x.from%>');
<%}}) %>
/**
 * @class
 */
declare class <%=locals.name%> extends <%=locals.inherits ? locals.inherits : "DataObject" %> {
<% locals.attributes.filter(function(x) { return x.model === name; }).forEach(function(x) { %>
     <% if (x.description) { %>
     /**
      * @description <%-x.description%>
      */<%}%>
     public <%-x.property || x.name%><%-x.nullable ? '?' : ''%>: <%-x.typeName%>; <% }) %>

}

export = <%=locals.name%>;