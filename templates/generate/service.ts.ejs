import {ApplicationService, IApplication} from '@themost/common/app';

export class <%-name%>Service extends ApplicationService {
    /**
     * @param {IApplication} app
     */
    constructor(app: IApplication) {
        super(app);
    }
}
